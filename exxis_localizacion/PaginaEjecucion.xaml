<Page x:Class="exxis_localizacion.PaginaEjecucion"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:exxis_localizacion"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PaginaEjecucion" Loaded="Page_Loaded">
    <Grid Background="#EEEEEE">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right">
            <TextBlock Text="Conectado a " FontSize="12"  TextAlignment="Right" VerticalAlignment="Center"></TextBlock>
            <TextBlock Name="lblSociedad" Text="{Binding NombreSociedad, Mode=OneWay}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"></TextBlock>
        </StackPanel>
        <TextBlock Text="{Binding Path=ProgresoGeneralTexto}" Grid.Row="2" Grid.Column="1"></TextBlock>
        <ProgressBar Name="prbProgresoGen" Grid.Row="3" Grid.Column="1" Value="0"/>
        <TextBlock Text="{Binding Path=ProgresoParcialTexto}" Grid.Row="5" Grid.Column="1"></TextBlock>
        <ProgressBar Name="prbProgresoPar" Grid.Row="6" Grid.Column="1" Value="0"/>
        <TextBox Name="txtResultados" Grid.Row="8" Grid.Column="1" Text="{Binding Path=TextoResultados}" IsReadOnly="True" 
                    TextWrapping="WrapWithOverflow" AcceptsReturn="True" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12"
                 ></TextBox>
        <StackPanel Grid.Column="1" Grid.Row="10" Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button x:Name="btnSiguiente" Width="100" Click="btnSiguiente_Click" IsEnabled="False">Finalizar</Button>
        </StackPanel>
    </Grid>
</Page>
